# ======================================================================
# CONFIGURAZIONE AMBIENTE DI SVILUPPO
# ======================================================================
# Questo file contiene tutte le variabili d'ambiente per l'ambiente 
# di sviluppo locale. Viene caricato automaticamente quando ENV=development
# ======================================================================

# Specifica l'ambiente corrente
ENV=development
STRICT_VALIDATION=False  # Attiva la validazione se in produzione

# ======================================================================
# CONFIGURAZIONE DATABASE
# ======================================================================

DB_HOST=localhost                 # Host del database (localhost per sviluppo)
DB_PORT=3306                      # Porta MySQL standard
DB_USER=root                      # Utente del database (root per sviluppo)
DB_PASSWORD=your_dev_password     # Password del database
DB_NAME=reminder_app_dev          # Nome del database di sviluppo

# Configurazione pool di connessioni
DB_POOL_SIZE=5                    
DB_MAX_OVERFLOW=10                
DB_POOL_TIMEOUT=30                

# ======================================================================
# SICUREZZA
# ======================================================================
# ATTENZIONE: Utilizzare una chiave diversa in produzione!
SECRET_KEY=dev-secret-key-for-local-testing-only  # Chiave per JWT e crittografia

# ======================================================================
# CONFIGURAZIONE API
# ======================================================================
API_V1_STR=/api/v1                # Prefisso per le API v1

# ======================================================================
# CONFIGURAZIONE FRONTEND
# ======================================================================
# Attenzione: il valore deve essere in formato JSON valido
CORS_ORIGINS=["http://localhost:4200","http://localhost:3000"]

# ======================================================================
# CONFIGURAZIONE STORAGE
# ======================================================================
# 'local' per sviluppo, 's3' per produzione
STORAGE_TYPE=local                # Usa il filesystem locale per lo sviluppo

# ======================================================================
# CONFIGURAZIONE EMAIL
# ======================================================================
# Usa Mailtrap per testing (cattura le email senza inviarle realmente)
SMTP_HOST=smtp.mailtrap.io        # Host SMTP di Mailtrap
SMTP_PORT=587                     # Porta SMTP (587 per TLS)
SMTP_USER=your_mailtrap_user      # Username Mailtrap
SMTP_PASSWORD=your_mailtrap_password  # Password Mailtrap
EMAIL_FROM=dev@reminderapp.com    # Indirizzo mittente per le email

# ======================================================================
# CONFIGURAZIONE STRIPE (AMBIENTE DI TEST)
# ======================================================================
# Utilizza le chiavi di test Stripe, non quelle di produzione!
STRIPE_API_KEY=your_stripe_test_key            # Chiave API Stripe di test
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_test_secret  # Segreto webhook di test
PAYMENT_SUCCESS_URL=http://localhost:4200/payment/success  # URL reindirizzamento pagamento riuscito
PAYMENT_CANCEL_URL=http://localhost:4200/payment/cancel    # URL reindirizzamento pagamento annullato

# ======================================================================
# CONFIGURAZIONE TWILIO (AMBIENTE DI TEST)
# ======================================================================
# Utilizza le credenziali di test Twilio, non quelle di produzione!
TWILIO_ACCOUNT_SID=your_twilio_test_sid        # SID account Twilio di test
TWILIO_AUTH_TOKEN=your_twilio_test_token       # Token auth Twilio di test
TWILIO_PHONE_NUMBER=your_twilio_test_phone     # Numero di telefono Twilio di test

# ======================================================================
# CONFIGURAZIONE LOGGING
# ======================================================================
# In sviluppo, usa il livello DEBUG per avere più informazioni
LOG_LEVEL=DEBUG                   # Livello di log (DEBUG, INFO, WARNING, ERROR, CRITICAL)
